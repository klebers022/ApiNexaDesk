# Arquivos legados

Esta pasta usa o nome antigo e não é lida pela Supabase CLI.

As duas alterações de `customers` foram incorporadas ao schema inicial em `../migrations`. Elas ficam aqui apenas como referência histórica e não devem ser executadas.
